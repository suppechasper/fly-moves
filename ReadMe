R scripts for analysing fly movments.
The R scripts requires several packages to be installed.
For running the example.procrustes.analysis.R script requires
abind, shapes, circular

For some of the other analyses the following packages can be required
MASS, RColorBrewer, cluster, hmm.discnp, depmix

For optimal transport the R package "mop" which is not currently available from
CRAN. The package "mop" also requires the CPLEX optimization library from IBM to
be installed on the system.

The file  example.procrustes.analysis.R  contains an example analysis for path
segments.

Further examples will be added for other types of anlayses. 
